<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | API Token
    |--------------------------------------------------------------------------
    |
    | JWT-токен из личного кабинета WebKassa (раздел API).
    | Запросы разрешены только с IP, привязанного к ключу.
    |
    */
    'token' => env('WEBKASSA_TOKEN', env('WEBKASSA_API_TOKEN', env('API_TOKEN'))),

    /*
    |--------------------------------------------------------------------------
    | API Base URL
    |--------------------------------------------------------------------------
    */
    'base_url' => env('WEBKASSA_BASE_URL', 'https://cabinet.webkassa.by/api'),

    /*
    |--------------------------------------------------------------------------
    | HTTP Timeout (seconds)
    |--------------------------------------------------------------------------
    */
    'timeout' => (int) env('WEBKASSA_TIMEOUT', 120),

    /*
    |--------------------------------------------------------------------------
    | Timezone
    |--------------------------------------------------------------------------
    |
    | Часовой пояс для отображения дат в отчётах.
    |
    */
    'timezone' => env('WEBKASSA_TIMEZONE', env('TIMEZONE', 'Europe/Minsk')),

    /*
    |--------------------------------------------------------------------------
    | Merchant (шапка Excel-отчёта)
    |--------------------------------------------------------------------------
    */
    'merchant' => [
        'name' => env('WEBKASSA_MERCHANT_NAME', env('MERCHANT_NAME', '')),
        'unp' => env('WEBKASSA_MERCHANT_UNP', env('MERCHANT_UNP', '')),
    ],

    /*
    |--------------------------------------------------------------------------
    | E-POS
    |--------------------------------------------------------------------------
    */
    'epos' => [
        'default_service' => env('WEBKASSA_EPOS_DEFAULT_SERVICE', env('EPOS_SERVICE_DEFAULT', 'Аренда')),
        'export_path' => env('WEBKASSA_EPOS_EXPORT_PATH', storage_path('app/webkassa')),
    ],
];
